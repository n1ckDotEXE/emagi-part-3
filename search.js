const emojis = require('./emojis.js');

const search = input => {
    emojis.filter(emoji => {
        emojis.name.includes(input.toLowerCase()) {
            return emojis.symbol;
        }})
}


module.exports = search;  